export default {
    // Usamos jsdom para simular un navegador (necesario para window, fetch, etc.)
    testEnvironment: 'jsdom',
    // Esta opción ayuda a Jest a trabajar con archivos .js como módulos
    transform: {},
};